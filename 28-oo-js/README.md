# Fetch

## How to run

If you don't already have json-server:

```bash
npm i -g json-server
```

Then, to run the app:

```bash
json-server --watch db.json
```

And finally, open the `index.html` file.
