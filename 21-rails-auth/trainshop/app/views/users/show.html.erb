<h1><%= @user.name %>'s ticket page</h1>


<h1> TICKET SECRET DATA FOR SECRET TRAVEL 🚉</h1>

<% if @admin_panel %>
  <h4> Seems like you're an admin!</h4>
  <h3>Admin panel:</h3>
  <ul>
    <% @admin_panel.each do |u| %>
      <li>
        <%= link_to u.name, u %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h4>You're not an admin.</h4>
<% end %>

<%= link_to 'go back to see all trains', trains_path %>
